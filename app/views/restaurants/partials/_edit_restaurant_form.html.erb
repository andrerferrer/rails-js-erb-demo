<%= text_field :restaurant, 
               :rename, 
               placeholder: "Rename #{restaurant.name}", 
               class: 'hidden',
               id: "edit-restaurant-form-#{restaurant.id}" %>